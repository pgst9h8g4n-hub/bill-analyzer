# 2026-08-25（续）

## 完成事项
- [x] 推送所有代码到 GitHub（origin/main）
- [x] 修复代码审查问题：
  - 新增 `src/lib/session.ts` 共享 session store，消除 9 处重复 localStorage 读写
  - 新增 `src/routes/+layout.svelte` 统一布局和底部导航
  - 修复 `expenses/+page.server.ts` 和 `stats/+page.server.ts` 中 user_id 硬编码为 0 的问题（改为客户端调用）
  - 修复 `db/stats.ts` 周统计 bug（只过滤下限，未过滤上限）
  - 修复 `db/budget.ts` 分类预算进度条写死 100% 的问题
  - 实现 `settings/+page.svelte` 修改密码和删除账户功能（此前仅为 TODO）
  - 实现 `export/+page.svelte` 筛选后导出功能
  - 修复首页"本月总支出"显示静态 ¥0.00，改为动态获取
  - 添加 DB 层 try-catch 错误处理（expenses.ts、stats.ts）
  - 提取 `netReduce` 函数消除重复求和逻辑
  - 修复 TypeScript strict 模式错误（any 类型、重复声明等）

## 待办事项
- [ ] 完善底部导航各 tab 路由高亮
- [ ] 补充 aria-label accessibility 改进（12 个警告）
- [ ] iPhone 真机测试 + 部署
- [ ] 编写 iOS 快捷指令脚本
